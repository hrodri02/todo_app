# Uncomment the next line to define a global platform for your project
platform :ios, '10.0'
use_frameworks!

target 'productivityTracker' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks

  # Pods for AFSample
  pod 'Alamofire', '~> 4.5'
  pod 'SwiftyJSON'
  pod 'Charts'
  pod 'GoogleSignIn'
end

